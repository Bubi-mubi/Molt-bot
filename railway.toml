[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm build && pnpm ui:build"

[deploy]
startCommand = "node scripts/railway-start.mjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
